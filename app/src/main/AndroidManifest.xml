<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.k.hilaris.alpha">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".Views.MainActivity"
            android:label="Sudoku Online">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Views.Front.FrontActivity"
            android:label="Main Menu">
        </activity>
        <activity
            android:name=".Views.SinglePlayer.SinglePlayerMenuActivity"
            android:label="Select Difficulty">
        </activity>
        <activity
            android:name=".Views.MultiPlayer.MultiPlayerMenuActivity"
            android:label="Online Session">
        </activity>
        <activity
            android:name=".Views.Sudoku.SudokuActivity"
            android:label="Sudoku">
        </activity>
    </application>

</manifest>